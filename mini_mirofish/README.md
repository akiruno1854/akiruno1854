# MiroFish超ミニマム版（学習用CLI）

ニュースや政策などの短文を入力すると、5人のエージェントが3ターンだけ反応し、最後に要約レポートを作る最小サンプルです。

## 実行

```bash
python -m mini_mirofish.main --text "政府が電気料金の補助を縮小すると発表した"
```

モックモード（API不要）が既定です。APIモードにする場合は `.env.example` を参考に環境変数を設定してください。
